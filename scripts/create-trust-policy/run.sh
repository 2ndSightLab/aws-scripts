#!/bin/bash

echo "Creating a trust policy for an IAM role."

# This script should prompt the user to enter the principals for a trust policy in a loop, looping until the user hits enter to stop adding more principals.

#in the loop as the user for the principal type. Enter user, role, or service or enter when done. Show the list of options and user can select 1, 2, or 3.

#Prompt for the principal value (arn for user, arn for role, or aws service) 
